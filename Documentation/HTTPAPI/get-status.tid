title: $:/plugins/OokTech/Bob/Documentation/HTTPAPI/get-status
tags: [[HTTP API]]
verb: GET
route: /api/status
response: A JSON object containing status information about the server
parameters:
description: get server status information

Get server status information

```
GET /status
```

Parameters:

* ''id'' - the current user session UUID

Response:

* 200 OK
*> `Content-Type: application/json`
*> Body: see below

The JSON data returned comprises the following properties:

* ''username'' - the username of the currently authenticated user. If undefined, the [[WebServer Parameter: anon-username]] is returned instead
* ''anonymous'' - true if the current user is anonymous
* ''read_only'' - true if the current user is restricted to read only access to the server
* ''session'' - the sesion object for the current user-sesion (see below)
** ''wikiName'' - the $tw.wikiName for the session
** ''url'' - the server endpoint URL of the session
** ''id'' - the UUID the session
** ''token'' - the current auth token for the session
** ''expires'' - when the current auth token expires
** ''user'' - the UUID of the currently authenticated user
** ''username'' - the username of the currently authenticated user
** ''access'' - the access level of the currently authenticated user
** ''isLoggedIn'' - the logged in status of the currently authenticated user
** ''isReadOnly'' - true if the current user is restricted to read only access to the current wiki
** ''isAnonymous'' - true if the current user is anonymous
* ''space'' - always contains the object `{recipe: "default"}`
* ''tiddlywiki_version''  - the current TiddlyWiki version

For example:

```
{
  "username": "",
  "anonymous": true,
  "read_only": false,
  "session": {
    "wikiName": "Root Wiki",
    "url": "wss://127.0.0.1:8080"
    "id": "00000000-0000-0000-0000-000000000000",
    "token": "00000000-0000-0000-0000-000000000000",
    "expires": 1614052150748,
    "user": "00000000-0000-0000-0000-000000000000",
    "username": "GUEST",
    "access": 0,
    "isLoggedIn": false,
    "isReadOnly": true,
    "isAnonymous": true,
  },
  "space": {
    "recipe": "default"
  },
  "tiddlywiki_version": "5.1.18"
}
```
